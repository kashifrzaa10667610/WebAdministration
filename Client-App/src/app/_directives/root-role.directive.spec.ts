import { RootRoleDirective } from './root-role.directive';

describe('RootRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new RootRoleDirective();
    expect(directive).toBeTruthy();
  });
});
